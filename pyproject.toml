[project]
name = "mnist-training"
version = "0.1.0"
description = "MNIST digit classification using PyTorch"
requires-python = ">=3.8"
dependencies = [
    "torch>=2.0.0",
    "torchvision>=0.15.0",
    "numpy>=1.21.0",
    "matplotlib>=3.4.0",
    "tqdm>=4.65.0",
    "aider-chat"
] 